Object.getOwnPropertyNames(base).map(p => global[p] = base [p])

const pointEQ = p1 => p2 => p1.x == p2.x && p1.y == p2.y 

const validMove = move => state => state.moves[0].x + move.x != 0 || state.move[0].y + move.y != 0

dropfirst

IDBCursorWithValue()

